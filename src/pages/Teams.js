import React from 'react';
import { Container, Typography, Box } from '@mui/material';

function Teams() {
  return (
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" component="h1" gutterBottom>
        Équipes
      </Typography>
      {/* Contenu de la page Teams */}
    </Container>
  );
}

export default Teams; 