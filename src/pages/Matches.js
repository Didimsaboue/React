import React from 'react';
import { Container, Typography, Box, dividerClasses } from '@mui/material';

function Matches() {
  return (
    
    <Container maxWidth="lg" sx={{ py: 4 }}>
      <Typography variant="h4" component="h1" gutterBottom>
        Matches
       
      </Typography>
      {/* Contenu de la page Matches */}
    </Container>
    
  );
}

export default Matches; 